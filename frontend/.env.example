# Frontend environment variables (Astro)
#
# Copy this file to .env for local development:
#   - Windows (PowerShell): Copy-Item .env.example .env
#   - Windows (CMD):       copy .env.example .env
#
# When deploying to Vercel, add these in Project Settings → Environment Variables.

# Base URL of the backend API.
# Local backend default from this repo: http://localhost:4000
PUBLIC_API_BASE=http://localhost:4000
